# Familiar 本地预览验收记录 v0.1

日期：2026-09-17。交付性质：Windows x64 本地开发预览，尚非完整首版正式发布。

## 已交付

- 独立 Runtime 与 Controller，分别拥有单实例锁；控制器不承载模型请求或宠物生命周期。
- 最小插件内核；平台、配置域、渲染、角色包、行为、会话、LLM、气泡和控制器均作为官方插件装配。
- 两套可替换 2D 占位角色，待机、互动、拖动、等待和睡眠动作。
- 按需唤出的文字输入条、头顶流式气泡、取消、收起、草稿保留与最近回复找回。没有聊天记录页面。
- 本地设置、角色和位置恢复，近期会话上下文保存；兼容接口的模型配置与连接测试。
- Windows 便携目录 `release/win-unpacked`，主入口 `Familiar.exe`，控制器入口为同一程序加 `--controller`。

## 已运行的验证

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| TypeScript `tsc --noEmit` | 通过 | 产品与内核严格类型检查 |
| Node 单元/集成测试 | 20 / 20 通过 | 作用域清理、依赖和权限检查、失败回滚、配置冲突、原子保存、角色包、坐标、SSE 中文分片、推理字段过滤、401/429、断流、超时、取消和气泡行为 |
| `npm run package:dir` | 通过 | Windows x64 便携目录构建；当前未签名，仍使用默认应用图标 |
| `npm run smoke:packaged` | 通过 | 对实际打包后的 EXE 使用 Playwright 操作真实 Electron 窗口；独立临时用户数据与本地 HTTP SSE 夹具 |

最终打包测试证据：`.artifacts/packaged-smoke-1789649574859/result.json`，同目录保留控制器、宠物、输入条、短气泡与长气泡截图，已人工检查这些截图。

窗口测试确认：

1. 模型未配置时启动宠物并加载角色。
2. 从控制器修改大小到 125%、切换角色并验证实际窗口尺寸与渲染结果。
3. 通过本地真实 HTTP SSE 请求执行连接测试。
4. 发送后输入条收起，宠物气泡持续接收文字；正常关闭控制器不取消回复。
5. 控制器重新连接并读回已保存设置；Esc 收起后草稿保留。
6. 停止回复有效；清空会话后气泡隐藏。
7. 强制结束的是控制器实际主进程，随后仍能从宠物发起新对话。
8. Runtime 重启恢复角色与设置；下一轮请求中存在之前的近期上下文。
9. 短回复使用约 340 × 146 DIP 的小气泡，长回复不截断正文、限定窗口高度并内部滚动。
10. 反复唤出文字输入条不累积尺寸漂移。

本机窗口有缩放取整误差，测试允许 1—2 DIP。显示器几何计算的单元测试不能替代真实拔屏、混合 DPI 的手工测试。

## 本轮修复的运行问题

- 退出请求等待启动过程结束后再逆序释放插件，避免启动/退出交错留下窗口和进程。
- 输入条与气泡没有常驻动画，对它们关闭后台帧节流，解决隐藏后再激活时输入窗口渲染无法及时恢复的问题；宠物动画仍保留后台节流。
- Windows 不可调整尺寸的窗口会锁定最小/最大尺寸。透明浮层改为允许程序调整尺寸，并拦截手动拉伸；定位使用位置更新，防止覆盖刚调整的尺寸，并避免反复处理 1 DIP 的取整差异。API 行为参考 [Electron BrowserWindow 官方文档](https://www.electronjs.org/docs/latest/api/browser-window)，具体结果以上述本机窗口断言为准。
- 清理构建输出后再复制 UI，打包产物不再残留旧聊天页面。

## 首次原型验收当时的未完成项（历史记录）

- 尚未配置或请求真实 LLM 供应商；模拟接口通过不等于供应商集成验证通过。首次原型验收仅使用兼容流式接口；新增七种适配器的验证见下方后续记录。
- 中文字符串发送已验证；中文输入法候选选择、实际组合输入仍需手工验证。
- 多实体显示器、混合 DPI、拔屏恢复、全屏应用策略，以及鼠标透明区穿透的长期使用体验尚需专项验证。
- 自启动开关已实现，但未修改本机登录启动配置，也未执行重登验证。
- 尚未进行 2 小时待机/重复交互资源趋势测试。证据中的内存数值只是主进程 RSS 快照，不代表整个应用进程树，也不能作为资源达标结论。
- 未实现语音、Agent 工具、命令执行、3D Renderer、第三方插件安装/沙箱、完整插件热替换和正式安装器。

下一步先用一个真实兼容模型进行体验验证，再集中处理桌面手工验收与角色美术，不提前扩展 Agent 功能。

## 2026-09-17 后续修正：文字快捷键与源码交付

- 根据用户习惯改为 Enter 换行、Shift+Enter 发送，保留点击发送按钮。输入条提示、README 和交互文档已同步。
- README 只保留用法、源码运行、构建与检查命令；架构、数据说明和后续工作转到文档目录与继续开发指南。
- 用户取消本轮安装包构建，交付范围改为源码仓库；没有新增安装包构建、安装或 Release 发布。
- `npm run check` 通过：类型检查、20 项逻辑测试及开发构建。
- `npm run smoke` 通过：新增真实键盘 Enter 换行、不发请求、Shift+Enter 携带多行文字发送的断言；模拟 `isComposing` 事件不发送。其余气泡、发送按钮、进程独立性和重启流程继续通过。
- 本轮本地证据为 `.artifacts/smoke-1789650425347/result.json`；实际中文输入法候选选择仍属于手工验证项。构建产物、测试数据、密钥与本机快捷方式不纳入源码仓库。

## 2026-09-17 后续修正：多供应商、会话保持与头顶输入

- 七种供应商通过独立插件接入，使用各自原生协议；注册表、路由、模型目录、参数能力和会话预算均位于插件层，内核未改动。
- 控制器增加服务选择、自动模型目录、搜索/刷新/手填、按服务保存配置、按地址隔离密钥、温度/Top P、思考强度/预算、输出/上下文上限和超时。
- 稳定会话 ID、请求前缀、缓存提示及服务端用量读取；取消固定 200 条/12000 字符历史限制，按窗口预算移除最旧完整轮次。原生状态不向气泡展示，更换服务/凭据隔离重放。
- 输入与回复共用头顶锚点，编辑时隐藏回复，发送或收起后恢复；草稿和快捷键保留。真实窗口测试发现并修复了启动早期唤出输入被初始化隐藏覆盖的竞态。
- `tsc --noEmit`、35 项 Node 单元/协议/会话测试和开发构建通过。覆盖七种协议、推理过滤、原生状态、分页模型列表、截断/断流、目录加载取消、长会话、重启、裁剪、凭据作用域和启动竞态。
- 真实 Electron 烟雾测试通过，证据：`.artifacts/smoke-1789653500164/result.json`。新增验证七种服务选择、自动列表/搜索、参数隐藏联动、密钥隔离与系统加密、旧单密钥迁移、输入/回复头顶底边对齐、编辑互斥、会话 ID/前缀版本跨重启保持。已查看模型设置与重启输入截图。
- 没有调用真实云端模型或实际 llama-server，缓存读写字段使用协议夹具验证；不能据此声称实际命中率或节省金额。未重新构建便携目录或安装包，旧 `release/` 不代表当前源码；开发请使用 README 的源码运行命令。

## 2026-09-18：记忆、Skill、MCP 与旧入口修正

- 两个项目快捷方式原来仍指向旧 `release/win-unpacked/Familiar.exe`。已备份并改为当前源码开发入口，构建到 dist 后立即使用；没有重新打包安装器。顶部空间不足时，输入和回复也保持同一锚点，不再分别跳到侧面。
- 修复模型目录请求与参数检查共享状态导致的竞态：获取列表期间调整温度或模型参数，不会使目录结果失效；缺密钥时仍正确更新参数能力。真实窗口验证自动获取、搜索、刷新、保存密钥、旧密钥格式迁移与服务切换。
- 新增独立的 home-files、memory-files、skills-local、mcp-client、agent-tools、tools-basic、agent-runner、companion-controls 插件。Kernel 未添加业务。Runtime 实际运行 33 个插件，Controller 独立运行。
- 核心记忆 3000 Unicode 字符边界、模型精炼/失败回退/原文保留、取消不迟到写入、每日追加与去重、完整长回复保留及分段检索、重启恢复和清空聊天不清记忆均有测试。
- Skill 规范校验、目录/正文安装、二进制资源原样保留、停用、移除备份与重启不恢复；MCP stdio 初始化、工具目录、环境密钥、具体确认和停用撤销均通过。
- 七种模型协议的工具调用流、参数拼接、隐藏原生推理/签名、工具结果回传和最终回复已使用协议夹具验证。工具轨迹不显示在气泡，完成工具后及时持久化；取消/错误轮次保留记录但不回放。
- 最终 `tsc --noEmit`、51 项 Node 测试、开发构建、`smoke` 和 `smoke:agent` 通过。证据：`.artifacts/smoke-1789734857385/result.json`、`.artifacts/agent-smoke-1789734868063/result.json`；已检查记忆、Skill、MCP 和命令确认的真实窗口截图。
- Exa 官方 Streamable HTTP 连接实测通过，列出 `web_search_exa`、`web_fetch_exa`。证据：`.artifacts/agent-smoke-1789734271368/result.json`。这项验证覆盖初始化和真实工具目录，不代表付费搜索额度或所有网页读取均已实测。
- 依赖审计曾发现 yaml 2.8.2 的已知漏洞，已升级到 2.9.1，npm audit 报告 0 个漏洞。

### 原数据与真实模型验证边界

开发前备份在 `.artifacts/user-data-backup-20260918-194746/`，包含 settings、conversation、原密文、Runtime 的 Local State 和旧快捷方式。源码版第一次正常启动前，原 settings、conversation、model-secret 与备份逐项一致。源码版启动只将旧密钥字符串迁移为按作用域保存的结构，原密文字节已验证保留。

用户允许使用已保存 API。实际验证发现，现有密文无法被系统密钥库解密；旧版程序原目录、隔离副本及最终非 Playwright 启动的源码版均未能解密。Playwright 的 Electron 启动器本身还会带 mock-keychain，因此真实凭据验证脚本改为普通进程启动并通过 CDP 连接，排除了这个测试干扰。未认定密钥失效的具体历史原因，未删除或用空值覆盖原密文，也未将密钥输出到日志。

上述为当时的验证结果；后续已保存密钥的实际调用结果见下节。`scripts/verify-live.mjs --saved-api` 只在获得授权后运行，复制加密材料到隔离目录，不使用原聊天内容。其他供应商、真实 llama-server、所有 npm MCP 包安装兼容性、长时待机、多屏 DPI 和实际中文输入法仍需后续专项验证。

## 2026-09-18：日常陪伴与设置保存修复

- 角色、大小及气泡秒数自动保存、即时应用；气泡默认 15 秒，支持 0 保持显示。历史会话管理可查阅、搜索、新建、重命名、删除、继续，模型上下文裁剪不再删除历史原文。控制器中交谈时抑制宠物气泡。
- 工具权限支持分类默认选择、仅本次、本会话同工具、精确白名单和全自动。读取、搜索、宠物目录内写入默认允许；目录外路径仍可使用，按策略确认。
- 主动关怀保留一个总开关，开机、工作、早中晚内置询问不逐条展示；用户自定义提醒支持添加、勾选和移除。问候调用 LLM，写入当前历史和日记；设备定位、高德附近建议单独授权，未授权不推测位置。
- 新增 companion-care、amap-care、location-windows、experience-controls 四个插件，实际 Runtime 共 37 个插件均运行正常，Kernel 未添加产品业务。
- Windows 登录项修正了带空格路径转义和自定义名称读回。通过独立临时注册项执行真实写入、读回与清理，用户原 Familiar 登录项保持启用。未实际注销或重启 Windows。

### 验证证据

| 验证 | 结果与证据 |
| --- | --- |
| 类型检查、开发构建 | 均通过；含 Windows 定位原生辅助程序编译 |
| 单元、协议、集成测试 | 58 / 58 通过，增加权限持久化、外部文件访问、历史完整性、关怀调度、自定义提醒、位置来源和高德字段约束 |
| 基础真实窗口测试 | `.artifacts/smoke-1789737807343/result.json`；气泡、快捷键、实时外观、模型参数、密钥、进程独立性和重启恢复 |
| Agent 真实窗口测试 | `.artifacts/agent-smoke-1789737967115/result.json`；记忆、Skill、stdio MCP、工具往返和审批 |
| 新体验真实窗口测试 | `.artifacts/experience-smoke-1789739254834/result.json`；权限保存、会话与白名单批准、应用内聊天抑制气泡、气泡计时、自定义提醒、高德配置和密钥隔离。已检查 care/history/permissions 截图 |
| 当前已保存 API | `.artifacts/live-api-1789738337510/result.json`；兼容接口实际返回 2 个模型，deepseek-flash 完成记忆工具调用和检索；Exa 官方目录列出 2 个工具 |
| 真实 LLM 主动问候 | `.artifacts/live-api-1789739314160/result.json`；同一已保存 API 成功生成问候，无定位授权时未取得位置 |
| 定位辅助程序加载 | `.artifacts/location-self-test.out`；`--self-test` 返回 Geolocator 类型且退出码 0，没有请求或读取用户位置 |

### 用户实际进程中的保存故障

用户报告 `Cannot read properties of undefined (reading 'settings')` 和保存时“命令不允许”。实际检查发现后台仍是旧的 33 插件进程，新版控制器连接它后，`policy.get`、`care.get` 被旧命令白名单拒绝；这与 MCP 是否配置无关。

已新增接口版本检查与关怀响应校验。完整备份当前 data、Runtime 的 Local State 和宠物之家至 `.artifacts/repair-backup-1789739198807/` 后，正常退出两端并启动同一构建。实际数据上的 `policy.set`、`care.set` 保存和读回均通过，模型密钥仍可用，原密钥文件逐字节未变，历史迁移后可列出。记录见该目录 `verification-before.json` 与 `verification-after.json`。

本轮没有构建安装包。高德真实 Key 的周边搜索、路线与价格、Windows 系统位置授权、实际重登、长时待机与多屏体验仍未实测；对应夹具与原生组件加载成功不能代替这些验证。

### Windows 托盘图标

原托盘代码用单色 Buffer 创建 16×16 图片，所以隐藏图标栏里只有绿色色块。已替换为与控制器一致的奶白 `f.` 标识，矢量源为 `assets/familiar.svg`，生成包含 16、20、24、32、40、48、64、128、256 像素图层的 `assets/familiar.ico`。托盘和窗口都加载该图标；未来打包配置同步指定它，本轮不打包。格式选择参考 [Electron Tray 的 Windows 说明](https://www.electronjs.org/docs/latest/api/tray)。

已通过 Windows 原生 `LoadImage` 验证全部九种尺寸，查看系统加载出的 24 像素图标，能辨认标识且四角透明；没有实际操作 Windows 隐藏图标面板。类型检查、开发构建及真实窗口回归通过，最新证据 `.artifacts/smoke-1789739698718/result.json`。已使用原有数据启动此构建。

## 2026-09-18：v0.1.0 首次发行准备

用户明确授权同步源码、发布第一版，并将仓库改为公开。发行定位为 Windows x64 预览版，构建 `Familiar-0.1.0-Setup-x64.exe` 和 `Familiar-0.1.0-win-x64.zip`；不包含用户模型密钥、配置、聊天、记忆或测试数据。

- 保留 EXE 图标和版本资源编辑，只关闭代码签名。已从生成的 EXE 中提取并检查 `f.` 图标，读取产品名 Familiar、版本 0.1.0。
- 安装版通过安装器标记使用 `%APPDATA%/Familiar/home`，避免记忆位于会被安装器删除的程序目录。免安装版保留优先放在程序旁的方式；环境覆盖仍有效。
- `tsc --noEmit`、58 项 Node 测试及开发构建通过。NSIS 安装程序、ZIP 和校验文件已生成。
- 打包后的三个真实窗口测试全部通过：`.artifacts/packaged-smoke-1789742388128/result.json`、`.artifacts/packaged-agent-smoke-1789742464401/result.json`、`.artifacts/packaged-experience-smoke-1789742508312/result.json`。覆盖进程分离、气泡、模型列表、密钥、记忆、Skill、MCP、Shell 审批、权限保存、历史会话、关怀和自定义提醒。
- 打包后的定位辅助程序 `--self-test` 退出码 0，能加载 Windows Geolocator；没有请求真实定位。
- 检查源码与旧提交历史，未发现运行目录或常见明文密钥；检查 app.asar（1681 项）和 ZIP（79 项），未发现用户数据。ZIP 内 EXE、app.asar、定位辅助程序的 SHA-256 与实际测试目录一致。依赖许可证随发行包保留。
- 生成 `SHA256SUMS.txt`：安装包 `5e8cfe6932970008f6ce5107f2f2209134c3d19d5ebe37316db6d8a35ba91e77`，ZIP `1980dd1684b67a19b002359ff9c00140d42028cb24787c0dca1367a1d670ce23`。

安装器向导、真实安装/升级/卸载流程未在本机执行，不把打包成功等同于该流程验收；实际定位、高德、注销重登及其他既有未验证项仍适用。运行方式、迁移与开发发布步骤见 [v0.1.0 说明](releases/v0.1.0.md)。
